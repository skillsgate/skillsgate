# Neon Postgres connection strings
# Pooled connection (for application queries via HTTP adapter)
DATABASE_URL="postgresql://user:password@ep-example-123456.us-east-2.aws.neon.tech/openskills?sslmode=require"

# Direct connection (for Prisma migrations — bypasses connection pooler)
DIRECT_DATABASE_URL="postgresql://user:password@ep-example-123456.us-east-2.aws.neon.tech/openskills?sslmode=require"
